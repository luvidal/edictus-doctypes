# jogi-doctypes
Chilean document-type catalog — single source of truth (doctypes.yaml source + build compiler + generated JSON + helpers/types + multipart) for the Jogi classifier/extract/cédula engine, host, and future document-reader SaaS.
